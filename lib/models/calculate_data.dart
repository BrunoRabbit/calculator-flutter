// could create a map but i like to use a class model
class CalculateData {
  CalculateData({
    required this.history,
    required this.day,
    required this.hour,
  });

  String history;
  String day;
  String hour;
}
